import React, { Component, PropTypes } from 'react'

export default class DivIcon extends Component {
    static propsTypes = {
        iconSize: PropTypes.array
    };
    render() {
        return <noscript></noscript>;
    }
}
